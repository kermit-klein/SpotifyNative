import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    color: "white",
  },
  artist: {
    color: "lightgrey",
  },
  image: {
    width: 75,
    height: 75,
  },
});

export default styles;
