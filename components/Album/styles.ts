import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: 200,
  },
  image: {
    width: "100%",
    height: 200,
  },
  text: {
    marginTop: 10,
    color: "grey",
  },
});

export default styles;
